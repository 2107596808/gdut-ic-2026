# 2048（阶段一：能自己玩 / 阶段二：AI 自动演示）

> 交付物就是 **一个文件**：`game/index.html`。浏览器双击打开即可，无框架、无构建、不联网。
> 本文件说明「怎么实现的」「怎么验证的」「给 AI 的关键提示词与迭代过程」。
>
> **先说清楚分工**：这份代码是 AI 写的，我负责提需求、定验收标准、跑测试、判断结果对不对。
> 下面凡是说「发现/定位/修掉」的地方，指的都是「我和 AI 一起，通过跑脚本、看数据找出来的」，
> 不是我自己一行行敲出来的。我不想把 AI 的功劳写成自己的——
> 评审要是当面问代码细节，我也只能讲我真正搞懂的那部分，不如一开始就写明白。

---

## 1. 怎么打开、怎么玩

```
双击 game/index.html（或拖进浏览器）
```

| 操作 | 方式 |
| --- | --- |
| 移动 | 键盘 `↑ ↓ ← →` 或 `W A S D`；鼠标按住棋盘拖拽；手机直接滑动 |
| 新一局 | 「新一局」按钮（**不清空**最高分与局数） |
| 清空记录 | 「重置记录」按钮（最高分 / 局数 / 战绩一起清） |
| 换配色 | 「🌗 切换主题」按钮，刷新后保留 |
| AI 演示 | 「▶ AI 自动演示」，全程不需要人工 |
| AI 单步 | 「单步 AI」按钮，自己控制节奏 |
| AI 棋力 | 右侧下拉框 4 档（闪电 / 标准 / 强 / 极强），决定**每步搜多深** |
| 演示速度 | 右侧滑块 1~60 步/秒，只管节奏，**不影响棋力** |

---

## 2. 阶段一：能自己玩的版本

### 实现

规则层是**纯函数**，完全不碰 DOM，所以能被 Node 直接调用做自动验收：

| 函数 | 作用 |
| --- | --- |
| `createGrid()` | 生成 16 格的一维棋盘（`0` 表示空） |
| `slideRow(row)` | 把一行左压 + 合并，返回新行与本次得分 |
| `applyMove(grid, dir)` | 四个方向的完整移动，返回新棋盘 / 得分 / 是否真的动了 / 合并位置 |
| `canMove(grid)` | 还有空格或还有相邻同值 → 游戏没结束 |
| `addRandomTile(grid, rand)` | 随机空格落一个方块（90% 是 2，10% 是 4） |

**合并规则**：一行里每个方块每一步只能合并一次。`[2,2,2,2]` 得到 `[4,4]` 而不是 `[8]`，
这条是 2048 最容易写错的地方，所以在验收脚本里专门有断言。

**三项新增功能**（任务书要求三选一，这里三项都做了，按验收标准逐条对照）：

| 新增功能 | 验收标准 | 本项目实现 |
| --- | --- | --- |
| 计分制 | 记录得分/最高分/局数；新一局不清零；有「重置」按钮 | 顶部分数栏 + 右侧状态栏；`localStorage` 持久化；「新一局」只重置当前局；「重置记录」单独清空记录 |
| 主题切换 | ≥2 套配色；一键即时生效；刷新后保留 | 深浅两套配色（CSS 变量 + `data-theme`）；点击立即生效；写入 `localStorage`，刷新恢复；首次打开跟随系统 `prefers-color-scheme` |
| 多局战绩 | 记录最近 10 局；新一局自动追加；可查看完整列表 | 每局结束写入战绩（得分/最大方块/步数/是否 AI 操作）；`pushRecord` 自动截断到 10 条；右侧面板列出完整列表 |

### 验证

```bash
node game/tests/verify_game.mjs          # 自动验收（不需要浏览器）
python game/tests/verify_static.py       # 单文件 / 无外链 / 功能点静态检查
```

`verify_game.mjs` 的做法值得说明：它**不复制一份逻辑来测**，而是用正则把 `index.html` 里的
`<script>` 源码抠出来，配上一套自制的「假 DOM + 假 localStorage」，丢进 Node 的 `vm` 里跑，
再通过 `window.Game2048`（逻辑层）和 `window.__2048__`（UI 桥接层）两个出口做断言。
所以它测的是**真正会跑在你浏览器里的那份代码**。

阶段一相关的断言（共 40 余项，节选）：

- `[2,2,2,2] → [4,4,0,0]` 且只加 8 分（不允许越级合并）
- 四个方向的移动结果都和参考实现逐格一致（随机推进 2500 步 × 4 方向 = 10000 次对比，零差异）
- 无效移动必须返回 `moved = false`（否则会凭空冒出新方块）
- 连续 40 步移动后，界面显示的分数与规则层累加的分数完全相等
- 新局渲染出 16 个背景格、分数显示同步
- 一键切换主题即时生效，并写入 `localStorage`
- 连开 12 局后战绩只剩最近 10 条，且保留的是最近的（脚本会核对首尾两条的具体分值）
- 「重置记录」后最高分/局数/战绩全部归零
- 键盘、鼠标拖拽两种输入都能操作棋盘

---

## 3. 阶段二：让 AI 来玩

### 3.1 AI 是怎么做的

**一句话**：AI 用「**角块启发式 + expectimax 搜索**」，评估函数照搬了开源实现
[qpwoeirut/2048-solver](https://github.com/qpwoeirut/2048-solver)（C++）里效果最好的那一套，
搜索部分按 JavaScript 的情况重写。

- **角块启发式（corner heuristic）**：给棋盘四个角各准备一张权重表，
  越靠近角的格子权重越大（10 / 5 / 2 / 1），把每格的「权重 × 方块值」加起来，四个角取最大。
  含义很直白：**把大块堆在角落里、并且沿着某一条边排开**。
- **expectimax**：
  - 我的回合 → 取 `max`（四个方向里挑期望最高的）
  - 随机落子回合 → 取**期望**：对每一个空格，分别按 90% 出 2、10% 出 4 计算加权平均
    （**全宽**遍历，不抽样——抽样会漏掉「棋盘被填满」的风险局面）
- **自适应深度 + 棋力档位上限**：深度用 `aiPickDepth`（照抄参考实现的 `pick_depth`）——
  局面越满、方块种类越多就搜得越深；再用「AI 棋力」档位压一个上限（默认「标准」= 3 层）。
  40 局实测：自适应（上限 3）的 4096 达成率 **78%**，优于固定 3 层的 70%。
  ⚠️ 这个上限**曾经**是挂在「演示速度」滑块上的，那是个设计错误（见 3.3.1），现已拆成独立下拉框。
  ⚠️ 中间试过「迭代加深 + 时间预算」（想让时间决定深度），实测是**负优化**：
  它每层贵约 10 倍，几十毫秒连第 4 层都搜不完，「只认完整搜完的层」于是把结果
  一直退回浅层，同时耗光预算。单局对比：迭代加深 40ms → 59 秒 / 最大块 2048；
  直接搜 3 层 40ms → 4.2 秒 / 最大块 4096。
  结论：**预算是上限、不是目标，用不满并不等于浪费**；关键是「比较的分数要完整」。
- **两个剪枝**（照搬参考实现，作用比想象中大）：
  - **`fours >= 4`**：同一条搜索路径上连出 4 个「4」的概率只有 0.01%，遇到就直接返回静态评估。
    砍掉的正好是最贵的那批深层叶子。
  - **提前终局检测**：在展开一个节点**之前**先判「还有没有合法走法」，没有就立刻给死亡惩罚
    （比正常评估低 1/4）。不这么做的话，死局要等展开完才发现，等于白搜一层。
- **平局裁决**：期望分相同时先看角块分，再看「蛇形字典序」，保证不会在等价走法之间乱抖。
- **置换表只在 `depth >= 2` 时读写**：浅层节点数量最多、复用价值最低，却要为每个节点
  付一次 Map 查找和写入——参考实现用 `CACHE_DEPTH = 2` 把这笔开销省掉了。

### 3.2 为什么要用位棋盘

把整个 4×4 棋盘压进**两个 32 位整数**（`lo` 前两行、`hi` 后两行，每格 4 bit 存 `log2(方块值)`，
也就是「等级」，0 表示空格）。这样「移动整块棋盘」就退化成几十次位运算 + 查表，
比数组版快一个数量级——而 AI 的棋力几乎完全取决于**单位时间能搜多少个节点**。

⚠️ 这里踩过两个必须记住的坑（都写在代码注释里了）：

1. **JS 位运算只有 32 位**：`1 << 48` 不会得到一个 64 位数，而是被截断成 `1 << 16`，
   而且**不报错**，只是默默算出错误的棋盘。
2. **不能用 `lo + hi * 2^32` 拼成一个大整数**：64 bit 的棋盘超过 JS 的
   安全整数上限（2⁵³），高位会被悄悄吃掉。需要唯一标识一个局面时只能拼字符串。
3. **置换表的键：64 bit 的棋盘 + 深度塞不进 53 bit，只能哈希，不能截字段。**
   这个键我前后改了三版，每一版都「看起来对」：

   | 版本 | 写法 | 问题 |
   | --- | --- | --- |
   | ① | `hi * 32 + depth` | **把 `lo` 整个丢了** —— 前两行不同的局面撞同一条缓存 |
   | ② | `(lo * 131072 + (hi >>> 15)) * 16 + depth` | 看着又快又稳，其实**丢了 `hi` 的低 15 位**（含整个第 3 行）。实测 400 步里出现 **10 次**「不同棋盘共用缓存」 |
   | ③ | 两个 `Math.imul` 混合全部 64 bit | 现在这一版，实测碰撞 **0 次** |

   关键在于：**棋盘是 64 bit、还要再塞 depth，而 `Number` 的安全整数只有 53 bit，
   信息一定装不下**。所以必须用哈希把 64 位均匀铺开，截掉任何一段字段都是在制造碰撞。
   （第 ② 版就是典型的「以为省了哈希、其实丢了数据」。而 ① 和 ② 的症状都只是
   「AI 莫名其妙下得差」，不会报错、不会崩溃，极难排查——所以我在验收里加了
   「位棋盘与数组层逐格一致」的断言，改键之后也专门写了脚本直接数碰撞次数。）

### 3.3 硬指标达成情况

**任务书硬指标是「合出 1024」—— 40 局里 39 局达成（98%）。**

下面的数字用**浏览器里「AI 自动演示」的同一套参数**跑
（棋力档位 = **标准**，即每步固定搜 3 层），
验收脚本也走同一条代码路径，所以它就是你点开演示会看到的水平。

> ⚠️ **「AI 棋力」和「演示速度」是分开的两维**（原委见 3.3.1）：
> 下拉框决定每步搜多深，滑块只决定多久走下一步——滑块**不再影响棋力**。
> 下面这张表是**标准档**的成绩；想拿更高分数就换「强 / 极强」档，代价是变慢。

| 指标 | 40 局结果 |
| --- | ---: |
| 合出 **1024**（硬指标） | **39/40 = 98%** |
| 合出 **2048**（加分项） | **39/40 = 98%** |
| 合出 4096 | 31/40 = 78% |
| 合出 8192 | 4/40 = 10% |
| 平均得分 | 65129 |
| 平均最大块 | 4006 |
| 平均单次决策 | **1.1 ms** |

最大方块分布：512 × 1、2048 × 8、4096 × 27、8192 × 4。

#### 3.3.1 棋力档位：把「搜多深」从「演示多快」里彻底拆出来

**先说踩过的坑（这是这一版最重要的改动）**：改之前，深度上限是**跟着演示速度滑块走的**——
速度拉到 ≥35 步/秒就只搜 2 层。同一批固定种子、同样 40 局，只改深度上限：

| 深度上限 | 当时对应的速度档 | 合出 2048 | 合出 4096 | 平均得分 | 平均单局 |
| ---: | --- | ---: | ---: | ---: | ---: |
| **3** | 8~34 步/秒（默认 20） | 39/40 = 98% | 31/40 = 78% | 65129 | 3.3 秒 |
| 2 | ≥35 步/秒 | 24/40 = 60% | 3/40 = 8% | 27730 | 0.1 秒 |

也就是说：**用户只想「看快一点」，代价却是 AI 悄悄变弱**（2048 从 98% 掉到 60%，
40 局里还有 2 局连 1024 都没到）。一个标着「速度」的控件不该改变棋力，
这是设计错误、不是调参问题，所以现在拆成两维：

| 控件 | 管什么 | 不管什么 |
| --- | --- | --- |
| **AI 棋力**（下拉框，4 档） | 每步搜多深 | 演示节奏 |
| **演示速度**（滑块 1~60 步/秒） | 多久走下一步 | 棋力 |

**四档实测**（走同一份代码路径；强 / 极强用同种子对照）：

| 档位 | 每步深度 | 单步耗时（实测） | 单局耗时 | 平均得分 | 合出 4096 | 合出 8192 |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| 闪电 | 固定 2 层 | ~0.1 ms | ~0.2 秒 | 未单独统计 | — | — |
| **标准**（默认） | 固定 3 层 | **1.1 ms** | **~4 秒** | 65129（40 局） | 31/40 = 78% | 4/40 = 10% |
| 强 | 固定 4 层 | 6.8 ms | 12~52 秒 | 94463（3 局） | 2/3 | 1/3 |
| 极强 | 固定 5 层 | 14.0 ms | 79~106 秒 | **160908（4 局）** | **4/4** | **4/4** |

「时间换分数」最直观的一组：**同一个种子 20260910 跑三档**

| 档位 | 得分 | 最大方块 | 步数 | 单局耗时 |
| --- | ---: | ---: | ---: | ---: |
| 标准 | 27560 | 2048 | 1444 | 2.0 秒 |
| 强 | 76536 | 4096 | 3504 | 24.6 秒 |
| 极强 | **176904** | **8192** | 7428 | 84.6 秒 |

```bash
node game/tests/verify_game.mjs --games 40                                # 标准档（上面主表的配置）
node game/tests/verify_game.mjs --games 3 --level strong                  # 强档
node game/tests/verify_game.mjs --games 3 --level max --max-ms 2400000    # 极强档（记得放大单局上限）
```

⚠️ 跑强档以上**必须加大 `--max-ms`**（单局时间上限，默认 180 秒）：极强档一局要 1~2 分钟，
不改上限会被掐断——脚本会把被掐断的局标出来并判失败，因为拿半局成绩当结论没有意义。

代价和收益是明摆着的：**分数翻 2.5 倍，换来的是单局从 4 秒变成 85 秒**。
所以默认档仍然是「标准」，演示起来不卡；想冲分就手动切到「极强」。

<details>
<summary>点开看 40 局逐局明细（固定种子，可复现）</summary>

| 局 | 随机种子 | 得分 | 最大方块 | 步数 | 用时(s) |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 1 | 20260910 | 27560 | 2048 | 1444 | 1.5 |
| 2 | 20365639 | 36304 | 2048 | 1887 | 2.1 |
| 3 | 20470368 | 80364 | 4096 | 3718 | 3.7 |
| 4 | 20575097 | 51868 | 4096 | 2369 | 3.0 |
| 5 | 20679826 | 123876 | 8192 | 5107 | 5.2 |
| 6 | 20784555 | 71636 | 4096 | 3291 | 3.9 |
| 7 | 20889284 | 60628 | 4096 | 2820 | 2.9 |
| 8 | 20994013 | 59252 | 4096 | 2728 | 3.4 |
| 9 | 21098742 | 52108 | 4096 | 2393 | 2.9 |
| 10 | 21203471 | 27252 | 2048 | 1417 | 2.0 |
| 11 | 21308200 | 71072 | 4096 | 3238 | 3.8 |
| 12 | 21412929 | 60660 | 4096 | 2831 | 3.7 |
| 13 | 21517658 | 73028 | 4096 | 3376 | 3.4 |
| 14 | 21622387 | 21444 | 2048 | 1070 | 1.3 |
| 15 | 21727116 | 36912 | 2048 | 1926 | 2.5 |
| 16 | 21831845 | 71700 | 4096 | 3280 | 3.8 |
| 17 | 21936574 | 56520 | 4096 | 2589 | 2.9 |
| 18 | 22041303 | 76296 | 4096 | 3486 | 3.3 |
| 19 | 22146032 | 57072 | 4096 | 2622 | 2.8 |
| 20 | 22250761 | 133552 | 8192 | 5647 | 5.2 |
| 21 | 22355490 | 46552 | 4096 | 2046 | 2.2 |
| 22 | 22460219 | 76576 | 4096 | 3507 | 3.5 |
| 23 | 22564948 | 80240 | 4096 | 3691 | 3.6 |
| 24 | 22669677 | 60416 | 4096 | 2795 | 2.9 |
| 25 | 22774406 | 60548 | 4096 | 2832 | 3.2 |
| 26 | 22879135 | 77092 | 4096 | 3540 | 3.8 |
| 27 | 22983864 | 55996 | 4096 | 2559 | 2.7 |
| 28 | 23088593 | 11552 | 512 | 758 | 0.7 |
| 29 | 23193322 | 36956 | 2048 | 1938 | 2.2 |
| 30 | 23298051 | 36260 | 2048 | 1876 | 2.2 |
| 31 | 23402780 | 60404 | 4096 | 2807 | 3.7 |
| 32 | 23507509 | 80484 | 4096 | 3731 | 3.7 |
| 33 | 23612238 | 60444 | 4096 | 2821 | 3.1 |
| 34 | 23716967 | 70692 | 4096 | 3203 | 3.9 |
| 35 | 23821696 | 147632 | 8192 | 6026 | 6.3 |
| 36 | 23926425 | 27540 | 2048 | 1420 | 1.9 |
| 37 | 24031154 | 71228 | 4096 | 3241 | 3.9 |
| 38 | 24135883 | 169736 | 8192 | 7085 | 7.3 |
| 39 | 24240612 | 53504 | 4096 | 2494 | 2.9 |
| 40 | 24345341 | 72196 | 4096 | 3327 | 3.4 |

</details>

#### 深度策略：为什么用「自适应 + 上限」而不是固定层数

`aiPickDepth(lo, hi)` 就是参考实现里的 `pick_depth`（公式照抄）：局面越满、方块种类越多，
返回的深度越大（2~8 层）——「简单局面少想、危险局面多想」，这个策略本身是合理的。

但它没考虑「我只有几毫秒预算」。实测同样 200 步：

| 策略 | 200 步耗时 | 平均单步 |
| --- | ---: | ---: |
| 纯自适应（不设上限，最深 8 层） | 8.6 秒 | 43 ms |
| 固定 3 层 | 0.7 秒 | 3.5 ms |

搜索每深一层大约贵 10 倍，纯自适应在浏览器里会明显卡顿。所以**保留自适应、但压一个上限**。
上限取多少是实测出来的（同一种子、timeMs=120）：

| 上限 | 平均单局 | 5 局平均分 | 5 局最大块 |
| ---: | ---: | ---: | --- |
| 2 | 0.1 秒 | 33138 | 4096,1024,1024,2048,2048 |
| **3** | **3.6 秒** | **63703** | **4096,4096,4096,4096,2048** ← 默认档（标准） |
| 4 | 102 秒 | — | 8192（单局 176388 分） |

（这张表只有 5 局，是当初选型用的；四档的完整复测见 3.3.1。）

**上限现在是用户在界面上选的**，写在 `AI_LEVELS` 里（`game/index.html`）：
下拉框的选项、档位说明、`aiStep()` 实际用的深度、验收脚本 `--level` 跑的深度，
**全部来自同一个数组**——照抄公式最怕的就是「界面上一套、代码里一套」，
所以这里只留一个数据源，加档位只改 `AI_LEVELS` 一处。

> 这段历史值得记一笔：上限**曾经**是跟着「演示速度」滑块走的（≥35 步/秒 → 只搜 2 层），
> 结果「拉快一点看演示」会顺手把 AI 拉弱（40 局实测 2048 从 98% 掉到 60%）。
> 那不是调参问题，是**把两个不相干的量塞进了一个控件**，所以现在拆成两维。

40 局对比（同样上限 3）：自适应 **4096 达成 78%**，而固定 3 层是 70% —— 自适应更好。

> **为什么写 40 局而不是 10 局**：我最初只跑 10 局，那 10 局**全部**合出了 2048、7 局到 4096，
> 看起来漂亮得多。但 10 局里 1 局就占 10%，比例数字没有意义。
> 换 40 局后数字降下来，这才是真实水平。
> 验收脚本默认跑 10 局、门槛 80%，够用来当回归测试，但**不足以用来对外宣称达成率**。

#### 和参考实现的差距（如实说）

评估与搜索参考了 [qpwoeirut/2048-solver](https://github.com/qpwoeirut/2048-solver)，
它公布的是 **500 局**里 4096 达成 99.4%、8192 达成 91.8%、16384 达成 34.6%。

| 指标 | 本项目（40 局） | 参考实现（500 局） |
| --- | ---: | ---: |
| 1024 | 98% | — |
| 2048 | **98%** | ~100% |
| 4096 | 78% | **99.4%** |
| 8192 | 10% | **91.8%** |

差距主要在 **8192 以上**。原因还是搜索深度：它是长时间离线跑的 C++，
可以放开 2~8 层的自适应深度慢慢算；浏览器演示档每步只有一两毫秒
（还要留时间给页面渲染），默认只能压到 3 层。

不过这个差距**现在是可选的**：把「AI 棋力」切到「极强」（固定 5 层、单步 14 ms），
4 局实测**全部合出 8192**、平均 160908 分，代价是单局约 85 秒（见 3.3.1）。
再往上（6 层）还能更快到 16384，但单局要几十分钟，作为「点开就能看的演示」不现实，
所以没做成界面档位——需要的话命令行加 `--max-depth 6` 自己跑。

**任务书的硬指标（1024）和加分项（2048）在标准档就已达标（98%）**；
想跟参考实现比 8192，就把档位调到「极强」，接受单局一分多钟。如实写在这里。

### 3.4 怎么验证的（三类验证）

1. **规则层正确性**：用 `slideRow`（已被单测钉死的参考实现）做基准，
   随机落子推进 2500 步，每一步都拿 4 个方向共 10000 次，逐格比对数组层和位棋盘层的移动结果 —— 零差异。
   这条断言是发现「右移查表少做了一次镜像」「shift ≥ 32 被截断」这两个 bug 的关键。
2. **AI 行为**：固定种子跑整局，统计达成率、平均步数、单次决策耗时；
   同一批种子每次结果必须一致（`makeRng` 是 xorshift32），否则说明引入了不确定因素。
3. **界面契约**：静态检查 HTML 里每个 `id` 与 JS 引用对得上、
   没有外链、恰好一个内联 `<script>`；再看键盘/鼠标事件有没有真的绑上。

```bash
node game/tests/verify_game.mjs                    # 77 项断言 + 10 局实测
node game/tests/verify_game.mjs --games 3 --quick  # 快速烟雾测试
python game/tests/verify_static.py                 # 静态契约检查
```

---

## 4. 关键提示词与迭代过程

> 任务书要求：「用 AI 做出来非常简单，关键看你怎么设计提示词——先想清楚需求，再一步步让 AI 实现」。
>
> 下面记的是每一轮「我想要什么 → 得到什么 → 提示词怎么改」。
> 技术细节是 AI 查的，我负责判断它说的站不站得住脚（第 6 节的「AI 说错但我查证过」就是其中一例）。

### 第 1 轮：把需求说清楚（而不是说“帮我做个 2048”）

**我给 AI 的提示词：**

> 用纯前端单文件（HTML/CSS/JS）做一个 2048，要求：
> 1. 无框架、无外部依赖、双击打开就能玩
> 2. 键盘方向键 + WASD、鼠标拖拽、触屏滑动都能操作
> 3. 界面常显：得分、最高分、步数、最大方块
> 4. 另外加三个功能：计分制（得分/最高分/局数，新局不清零，有重置按钮）、
>    主题切换（深浅两套配色，刷新后保留）、多局战绩（记录最近 10 局）
> 5. **重要**：把纯逻辑（移动、合并、判负）和 DOM 操作分开，逻辑层导出成
>    `window.Game2048`，我要用 Node 直接调它做自动验收；UI 层另外导出 `window.__2048__`
> 6. 注释全部写中文，关键算法要解释“为什么这么写”

**结果**：一次就拿到了结构正确的单文件，逻辑层/UI 层分离也做到了。
**这次成功的经验**：第 5 条是关键——把「我要怎么验收」一起写进提示词，AI 才会把代码切成可测的形状。
后来验收脚本能在 Node 里跑真实代码，就是这一步埋下的。

### 第 2 轮：AI 方向（第一次尝试失败）

**我给 AI 的提示词：**

> 加一个「AI 自动演示」：点一下就不需要任何人工操作，一直玩到合出 1024。
> 用很小的代码量实现就行（贪心 + 评分函数，评分看空格数和可合并对）。

**结果**：AI 能跑，但很快就把棋盘塞满，最高只到 128 左右。
**问题定位（我自己读代码 + 加日志看出来的）**：贪心只看一步，遇到「这一步能合并、但那一步能保命」时
会选前者，于是越玩越乱。
**提示词怎么调整**：不再要求「用很小的代码量」，而是明确要求**搜索**：
「我方回合取 max，随机落子回合对每个空格按 90% 出 2 / 10% 出 4 求期望，也就是 expectimax」。

### 第 3 轮：明确要求参考成熟实现

**我给 AI 的提示词：**

> 参考 https://github.com/qpwoeirut/2048-solver 里效果最好的方案：
> 评估函数用 corner heuristic（四个角各一张 10/5/2/1 权重表，取最大），
> 搜索用 expectimax，深度按局面自适应（越满越深），平局用蛇形字典序裁决。
> **搜索部分要用位棋盘实现**：把 4×4 棋盘压成整数，每格 4 bit 存 log2(方块值)。
> 但注意 JS 的位运算只有 32 位，请把棋盘拆成 lo/hi 两个 32 位整数。

**结果**：结构对了，但棋力反而更差（最高 256）。
**这一步我做的事**：写了个把「位棋盘移动」和「数组层移动」逐格对比的脚本，
一跑就发现两者不一致 —— 于是找到了后续一串 bug。
**提示词怎么调整**：加了硬约束「**每写完一块，必须能用一个纯函数脚本证明它和数组层结果一致**」。
后面每次改 AI，我都先跑这个等价性对比。

### 第 4 轮：靠数据定位，而不是靠继续加要求

**我给 AI 的提示词（第 4 轮起基本不再改需求，改成“这是数据，请解释”）：**

> 现在 5 局固定种子跑下来，最高方块只有 256；同样的评估函数，把深度改成 4/5/6 反而更差。
> 这是不符合常理的（搜得越深不应该越差）。请检查：
> 1. 截断时返回的值会不会污染父层的比较？
> 2. 时间预算是不是被第一个方向吃光了，导致后面方向只搜到一层、和深搜值混在一起比大小？
> 3. 置换表的 key 有没有把深度考虑进去？

**结果**：定位到三个具体缺陷，逐个修掉后棋力从「最高 256」跳到「稳定 1024、偶尔 2048」：

| 缺陷 | 后果 | 修法 |
| --- | --- | --- |
| 四个方向共用一份时间预算 | 第一个方向花光时间，后三个只搜 1 层，等于拿「深搜值」和「静态分」比大小 | 每个方向发等额预算，整层共享 deadline |
| 预算耗尽只是 `return` | 父层循环仍把剩余兄弟节点全部走完，退栈代价 O(剩余节点数)：实测 `timeMs=10` 要跑 648 ms | 预算耗尽处 `break`，退栈回到 O(深度) |
| 被截断的局面也写进置换表 | 残缺值被缓存 → AI 系统性躲开「刚好被截断」的走法，偶发崩盘 | 截断的节点不写缓存；并改成**迭代加深**：只认「四个方向都完整搜完」的那一层 |

**这一轮最大的收获**：AI 提示词里写「请检查 X、Y、Z」比写「再聪明一点」有用得多。
把观察到的异常现象（“搜得更深反而更差”）和可能的机理一起给它，它才能定位到真问题。

### 第 5 轮：交给验收脚本定标准

**我给 AI 的提示词：**

> 把验收脚本补全：固定 10 个种子各跑一整局（全程无人工），统计 1024 达成率、平均得分、
> 平均步数、单次决策耗时，并且**低于 80% 达成率就直接退出码 1**。
> 另外把我的「阶段一三项新增功能」也逐条写成断言（计分制 / 主题切换 / 多局战绩）。

**结果**：就是现在 `game/tests/verify_game.mjs` 的样子——77 项断言，全部通过。

---

## 5. 防「纯粘贴」：两处关键逻辑，用我自己的话讲

### 5.1 为什么「搜索到一半」会让 AI 变笨？（迭代加深是怎么救回来的）

AI 每个回合要比较四个方向的「期望分」，然后挑最大的那个。

问题出在**时间不够的时候**：搜索是被一个时间预算掐断的，掐断时返回的这个分数
**不是完整搜索的结果**，而是一个「只算了一部分」的残缺值。而且它一定偏小——
因为期望是「求和 ÷ 空格数」，空格还没遍历完就退出了，分子只加了一部分，分母却是完整的。

于是发生了一件很隐蔽的事：假设「向左」这一格刚好把时间用光、被截断，得到 8000 分；
而「向右」完整搜完，得到 9000 分。那么 AI 会认为向右更好——**但这两个数根本不可比**，
一个是残缺值、一个是完整值。真实情况可能是向左本来有 12000 分。
更糟的是这个错误还会被缓存放大（残缺值写进置换表，后面一直读到它）。

修法是**迭代加深**：从 2 层开始，一层一层往下搜：
- 2 层：四个方向都完整搜完 → 记下这一层的结果
- 3 层：四个方向都完整搜完 → 覆盖上一层的结果
- 4 层：搜到一半时间到了 → **整层丢弃**，仍然用 3 层的结果

这样保证「拿来比较的分数永远是同一深度、且四个方向都完整搜索出来的」，
尺度一致，比较才有意义。同时时间也没浪费：局面简单时会自动多搜几层。

### 5.2 为什么评分函数是「角块权重 10/5/2/1」这种形状？

一开始我以为评分函数越「全面」越好，就把空格数、相邻同级对数、单调性、可合并对数
全都加权加起来。结果 AI 一直在原地打转，最高 256。

后来换了个角度想：**2048 里最值钱的资源不是分数，是「合并的顺序」**。
棋盘上真正危险的不是空格少，而是「大块散在中间、每个方向都动不了」。
如果一个 1024 待在角落里，无论往另外三个方向怎么滑，它都还在角落，周围的小块还能继续喂给它；
如果它跑到中间，你随便一动它就被挤走，之后想再凑一个 1024 就几乎不可能了。

所以真正该教的规则是：**大块必须待在角上，并且沿着一条边按从大到小排开**。
「权重 10 / 5 / 2 / 1」这张表就是把这句话翻译成数学：
离角最近的那格权重 10，沿边依次 5、2、1，中间和远端的格子权重是 0（那些位置放什么都不加分）。
四个角各算一次取最大值，等于允许 AI 自己选「这局主要经营哪个角」。

这个改动让 AI 从「最高 256」变成「稳定 1024」。
**教训**：评估函数的形状比项目数量重要得多——要写的是这个游戏的**策略**，
而不是把能想到的指标都堆上去。

---

## 6. AI 给过但我查证后认为是错误的信息

| AI 的说法 | 我的查证过程 | 结论 |
| --- | --- | --- |
| 「JS 里 `1 << 48` 可以像 C 的 `uint64_t` 那样把棋盘压成一个 64 位整数」 | 我按它的写法实现后，AI 每一步都在走坏棋。于是写脚本把「位棋盘移动结果」和「数组层移动结果」逐格对比，**立刻发现两者不一致**。查 JS 规范：位运算在 32 位有符号整数上进行，移位量会先对 32 取模，`1 << 48` 实际等于 `1 << 16`，**而且不报错** | ❌ 错的。改成 lo/hi 两个 32 位整数，移位一律控制在 32 位内；并在验收脚本里加了「位棋盘与数组层必须逐格一致」的断言，防止以后再犯 |
| 「用 `lo + hi * 2**32` 把两半合成一个整数当缓存键就够了」 | 实测 AI 又变差了。核对 JS 的 `Number.MAX_SAFE_INTEGER = 2^53 - 1`：64 bit 的棋盘远远超出，高位被静默丢弃 —— 也就是说**取出来的根本不是原来那个局面**，等于在缓存里随机撞键 | ❌ 错的。改成拼字符串做键（`lo + ':' + hi`）；搜索热路径上的整数键必须自己保证「各字段位宽不重叠、总和 ≤ 53 bit」，不能想当然地乘上去 |
| 「缓存键用位拼接就行，不用哈希，截掉低位也『基本够用』」 | 我按这个思路写了两版。第一版丢 `lo`，第二版丢 `hi` 的低 15 位。写脚本把「两个只差第 3 行的棋盘」拿去算键，**键完全相同**；再跑一局数碰撞，400 步里 10 次「不同棋盘共用缓存条目」。核对原因：64 bit 棋盘 + depth 塞进 53 bit 的安全整数，**信息必然装不下**，截字段就是在制造碰撞 | ❌ 错的。改回「用两个 `Math.imul` 把全部 64 bit 混进一个 53 bit 整数」，实测碰撞 0 次。这条和第 1 条（`1 << 48`）其实是同一个根源：**JS 的整数只有 53 bit 安全精度，任何「差不多够用」的假设都要先验证** |
| 「性能问题要先找出热点再优化」——我照着 profile 优化了两次 | 第一次 profile 指向 `mix32` 占 41%，换成位拼接后确实快了 3.8 倍，但棋力反而没提升多少。再查才发现真正的瓶颈是**时间预算只用了 1.4%**（`aiPickDepth` 当天花板锁死了搜索深度），跟热点无关 | ⚠️ 半对。profile 找的是「CPU 花在哪」，但**强不强取决于「这些 CPU 换来了多少搜索深度」**。两次测量的维度不同，第一次的「优化成功」差点掩盖了真正的瓶颈 |
| （任务书里给的）Croc 论文编号 `arXiv:2606.25673` | arXiv 编号格式是 `YYMM.NNNNN`，`2606` 表示 2026 年 6 月；用标题关键词检索时对不上，而任务书自己同时给了项目地址 `pulp-platform/croc`，用项目名才找得到正确入口 | ⚠️ 存疑，已按项目名检索为准。**查证过程比结论重要**：先看编号规则、再用标题反查，两条路对不上就不采信编号 |
| 「搜得越深一定越强」 | 用固定种子做 A/B：`depth=3` 能到 1024，`depth=4/5/6` 反而只到 256~512，而且更慢 | ❌ 在**有时间预算**的前提下不成立。原因是截断值污染了比较（见 5.1）。但要修的是「保证比较的分数都完整」，不一定是「加深」——我后来试的迭代加深反而更慢更浅（见下一条） |
| 「用迭代加深 + 时间预算，让时间自动决定搜多深」 | 按这个思路改完，实测单局 **59 秒**、最大块只到 **2048**；而固定 3 层只要 **4.2 秒**、能到 **4096** | ❌ 在这个预算量级下是负优化。迭代加深每层贵约 10 倍，几十毫秒连第 4 层都搜不完，「只认完整层」于是把结果一直退回浅层，同时耗光预算。**预算是上限、不是目标，用不满不等于浪费** |
| 「`Int32Array` 存棋盘就行，和普通数组一样用」 | 棋力一直比参考实现差。写脚本扫 6 万个随机棋盘核对「返回的方向是否真的改变了棋盘」，发现有的方向棋盘根本没变 | ❌ 错的。`Int32Array` 是**有符号**的：`0x80000000` 存进去读出来是负数，于是 `lLo !== lo` 恒为真，**棋盘没变的方向也成了合法走法**（幽灵走法）。触发条件是出现 ≥256 的方块（某半棋盘最高位置 1）。归一化后幽灵走法归零，4096 达成率 **43% → 70%** |

---

## 7. 玩法和实现上的一些细节

> ⚠️ **AI 演示时不弹全屏提示层**。合出 1024 的那一刻本来会弹一个盖住整个棋盘的提示，
> 但 AI 演示会忽略用户输入，那个提示就永远关不掉 —— 玩家看到的是「AI 停在 1024 不动了」，
> 实际上它还在下面继续跑（实测那局打到了 4096）。
> 现在 AI 演示时改成右上角飘一个提示，玩家自己玩时才弹可以选择「继续 / 重开」的层。



- **拖拽/滑动阈值 24 px**，低于这个值当点击处理，避免手一抖就动一步。
- **AI 演示时忽略键盘/鼠标输入**，保证「全程无需人工操作」这条硬指标不被误触破坏。
- **合出 1024 时弹出提示但不强制中断**，可以继续冲 2048；提示层在下一次操作时自动收起。
- **`localStorage` 不可用（隐私模式/被禁用）时自动降级为内存存储**，不会报错白屏。
- **大数字字号自动缩小**，1024 / 2048 不会溢出格子。
- 尊重系统的 `prefers-reduced-motion`：开了「减少动态效果」时关掉方块动画。

---

## 8. 自动验收覆盖了什么（77 项断言）

```
0. 单文件与 HTML↔JS 契约   6 项   只有内联脚本、无外链、不联网、id 引用对得上
1. 脚本可加载              2 项   逻辑层与 UI 桥接层都能导出
2. 核心规则               35 项   合并唯一性、四方向、判负、落子、可复现随机、位棋盘等价性、
                                 启发式与自适应深度、棋力档位（含「标准比闪电搜得深」的行为断言）
3. 阶段一界面与新增功能    29 项   渲染、计分、持久化、主题、战绩、重置、输入方式
4. 阶段二硬指标            5 项   10 局无人干预的达成率、步数、决策耗时、没有半局成绩混进来
                          ─────
                          合计 77 项
```

（跑「强 / 极强」档时是 76 项：那条「单步 < 120ms」的门槛只对标准档及更轻的档生效，
强档本来就是用时间换棋力的，不该被它卡住；与此同时「有没有被时间掐断」这条会一直生效。）

最后一条硬门槛：**1024 达成率低于 80% 直接以退出码 1 结束**，
也就是说这个脚本本身就是「AI 到底行不行」的裁判，不靠我自己说。
